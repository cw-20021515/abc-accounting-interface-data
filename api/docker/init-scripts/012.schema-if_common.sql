-- CREATE TABLE IF NOT EXISTS "if_approval_document"
-- (
--     "document_seq"    integer generated by default as identity primary key,
--     "template_seq"    integer                  not null constraint approval_document_fk_1 references approval_document_template,
--     "document_id"     uuid                     not null constraint approval_document_idx_1 unique,
--     "document_title"  varchar(100)             not null,
--     "content_html"    text                     not null,
--     "document_state"  varchar(50)              not null,
--     "deleted"         boolean                  not null,
--     "document_number" varchar(20),
--     "create_time"     timestamp with time zone not null,
--                                     "create_user"     varchar(32)              not null,
--     "update_time"     timestamp with time zone not null,
--                                     "update_user"     varchar(32)              not null
--     );
--
-- comment on table "if_approval_document" is '전자결재 기안 문서';
-- comment on column "if_approval_document".document_seq is '문서 시퀀스';
-- comment on column "if_approval_document".template_seq is '템플릿 시퀀스';
-- comment on column "if_approval_document".document_id is '문서 UUID';
-- comment on column "if_approval_document".document_title is '문서 제목';
-- comment on column "if_approval_document".content_html is '문서 내용 html';
-- comment on column "if_approval_document".document_state is '문서 상태';
-- comment on column "if_approval_document".deleted is '레코드 삭제 여부';
-- comment on column "if_approval_document".document_number is '문서 번호';
-- comment on column "if_approval_document".create_time is '생성 일시';
-- comment on column "if_approval_document".create_user is '생성 사용자';
-- comment on column "if_approval_document".update_time is '수정 일시';
-- comment on column "if_approval_document".update_user is '수정 사용자';
--
-- -- alter table approval_document owner to abc_commons;
-- -- grant select on approval_document to bigdata;
-- -- grant select on approval_document to athospipe;
--
